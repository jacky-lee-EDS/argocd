apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx-deployment
  namespace: nginx-helloworld
  labels:
    app: nginx
spec:
  replicas: 3
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
    spec:
      securityContext:
        fsGroup: 1001350000  # 設定 fsGroup，OpenShift 會調整權限
      containers:
      - name: nginx
        image: quay.io/ellery/hello-nginx:v0.6
        securityContext:
          allowPrivilegeEscalation: false
          runAsNonRoot: true
        ports:
        - containerPort: 80
        volumeMounts:
        - mountPath: /var/cache/nginx
          name: nginx-cache
        command: ["/bin/sh", "-c"]
        args:
          - mkdir -p /var/cache/nginx/client_temp && 
            exec nginx -g 'daemon off;'
      volumes:
      - name: nginx-cache
        emptyDir: {}
